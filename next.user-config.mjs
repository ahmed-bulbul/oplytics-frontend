/**
 * User-level Next.js config consumed by `next.config.mjs`. The wrapper
 * `next.config.mjs` file is auto-generated by v0 and expects this module
 * to exist with a default-exported config object that gets spread into the
 * final Next.js config. Without this file Next.js exits at startup with
 * `ERR_MODULE_NOT_FOUND` and the preview never comes up.
 */
const userConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
}

export default userConfig
